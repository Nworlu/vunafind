import React from 'react'
import './AuthBackgroundImage.css'

function AuthBackgroundImage({children}) {
  return (
    <div className='intropage-container'>{children}</div>
  )
}

export default AuthBackgroundImage